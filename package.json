{
	"name": "makutano-web",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"packageManager": "pnpm@10.0.0",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"dev": "pnpm --filter @makutano/shared build && pnpm --parallel --filter \"./apps/**\" dev",
		"dev:web": "pnpm --filter @makutano/web dev",
		"dev:api": "pnpm --filter @makutano/api dev",
		"build": "pnpm --filter @makutano/shared build && pnpm --filter @makutano/api build && pnpm --filter @makutano/web build",
		"start": "pnpm --parallel --filter \"./apps/**\" start",
		"check": "pnpm -r check",
		"clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/.svelte-kit apps/*/build apps/*/dist packages/*/dist"
	},
	"devDependencies": {
		"typescript": "^5.7.3"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"esbuild"
		]
	}
}
